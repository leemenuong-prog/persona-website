# macOS
.DS_Store

# Netlify CLI local state (deploy is via CI — see .github/workflows/deploy.yml)
.netlify/

# originals kept OUT of the repo (exceed GitHub's 100MB limit) — the repo
# ships compressed/web versions instead (see README). originals stay local.
*.zip
PeersWork-路演视频.mp4
作品集【李文苑】.pdf

# AI 作品集:母版与原始截图不入库(母版在 ../源文件/AI作品集/,原始截图在 ../产品-详情图/)
portfolio-ai-master*.pdf
ai-portfolio/raw/

# 本地工具配置
.claude/
